# Local and production secrets
config.php

# Guest uploads stay on the web server, not in source control
uploads/*
!uploads/.htaccess

# Generated offline albums remain on the web server, not in source control
albums/*
!albums/.htaccess

# Local/editor files
.DS_Store
Thumbs.db
*.log
